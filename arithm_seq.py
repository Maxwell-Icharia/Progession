def arith_geo(n):
	if n == list:
		return "Arrays only allowed"
		for x in b:
			if b == None:
				return 'should return 0 for an empty array'
			elif len[1] == len[0] + (len[1] - len[0]):
				return 'Is arithmetic'
			elif len[1] == len[0] * (len[1] / len[0]):
				return 'Is geometric'
			else:
				return '-1'